import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Here you can add additional global settings in the future
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


